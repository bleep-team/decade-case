import { History } from '@/components/history/history'

export const dynamic = 'force-dynamic'

/** The History page — the broker's full record of orders and trades. */
export default function HistoryPage() {
  return <History />
}
