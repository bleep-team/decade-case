{
  "scripts": {
    "setup": "cp $CONDUCTOR_ROOT_PATH/apps/web/.env apps/web/.env 2>/dev/null || true\ncp $CONDUCTOR_ROOT_PATH/packages/db/.env packages/db/.env 2>/dev/null || true\npnpm install\npnpm build"
  }
}
